import { DRippleDirective } from '../d-ripple.directive';

describe('DRippleDirective', () => {
  it('should create an instance', () => {
    const directive = new DRippleDirective();
    expect(directive).toBeTruthy();
  });
});
